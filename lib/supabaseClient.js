// import { createClient } from '@supabase/supabase-js';

// const supabaseUrl = process.env.NEXT_PUBLIC_CHORUS_SUPABASE_REST_ENDPOINT;
// const supabaseKey = process.env.NEXT_PUBLIC_CHORUS_SUPABASE_ANON_KEY;

// console.log(supabaseUrl);

// export const client = createClient(supabaseUrl, supabaseKey);
